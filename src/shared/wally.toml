[package]
name = "yadbro/my-work-game"
version = "0.5.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
authors = [
	"YadBro (https://github.com/YadBro)"
]
include = ["utils/**", "wally.toml", "wally.lock"]
exclude = ["**"]