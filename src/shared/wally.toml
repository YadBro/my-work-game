[package]
name = "yadbro/my-work-game"
version = "0.12.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
authors = [
	"YadBro (https://github.com/YadBro)"
]
include = ["utils/**", "wally.toml", "init.lua"]
exclude = ["**"]